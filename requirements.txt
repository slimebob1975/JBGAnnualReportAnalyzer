# Pinned versions for reproducible Docker builds.
#
# For development, prefer:  pip install -e ".[masking,ocr,dev]"
# which uses the ranges in pyproject.toml instead of these exact pins.
#
# These pins are the versions the current test suite was verified against.
# Regenerate with:  pip-compile pyproject.toml --extra masking --extra ocr

# --- core service -----------------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.38.0
starlette==1.6.0
jinja2==3.1.6
python-multipart==0.0.32

# --- model access -----------------------------------------------------------
openai==3.8.0
tiktoken==0.14.0

# --- PDF and output ---------------------------------------------------------
PyMuPDF==1.28.2
openpyxl==3.1.5

# --- optional: name masking -------------------------------------------------
# transformers does NOT depend on torch: torch is an optional backend that has
# to be named explicitly. Leaving it out gave
#   "None of PyTorch, TensorFlow >= 2.0, or Flax have been found"
# followed by NameError: name 'torch' is not defined, at the first masking call.
# Drop both lines, and set use_masking=no, for a slim deployment (~2 GB less).
transformers==4.57.1
torch==2.14.0

# --- optional: OCR for scanned reports --------------------------------------
# Requires the system packages tesseract-ocr, tesseract-ocr-swe, ghostscript.
ocrmypdf==17.11.0

# Removed: aiofiles, beautifulsoup4, lxml, requests, python-docx, pytesseract,
# tf-keras, pdfminer.six, pandas. Nothing in the codebase imports the first
# eight; pandas is now only needed for the optional [analysis] extra.
